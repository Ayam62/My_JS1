//alert(3+3) gives error becase we are in node woekspace and that syntax is only used in browser because the engine if JS is in the browser
let name="Ayam"//string
let age=19//number
let isLoggedIn=true //bool
// other datatypes =null
let state =null//empty for eg you have to send a n=tmperature but still the temp of that place is not found so you define it as
//null so that the consumer understads that the temperature is null so no any value is given


// to  view the type of any given value we have the syntax type of ...
console.table([typeof "Ayam",typeof 19,typeof true,typeof null])
// null is of object type and undefined is of undefined type.